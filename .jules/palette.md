## 2026-05-03 - Dashboard Input Headings
**Learning:** Using `<h3>` tags for form input visual labels breaks screen reader association.
**Action:** Always use semantic `<label>` tags with matching `for` attributes and use `class="block"` to preserve layout.
